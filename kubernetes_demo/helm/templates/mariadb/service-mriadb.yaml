apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: {{ .Values.bdd.name }}
  name: {{ .Values.bdd.name }}
spec:
  ports:
  - port: 3306
    protocol: TCP
    targetPort: 3306
  selector:
    app: {{ .Values.bdd.name }}
status:
  loadBalancer: {}